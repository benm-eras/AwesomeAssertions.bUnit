
@inherits Bunit.TestContext

@code {
    
    [Test]
    public void Succeed()
    {
        Render(@<a href="#" title="GitHub Repo">Link</a>)
            .Should().HaveTitle("GitHub Repo");
    }
    
    [Test]
    public void ReturnAndConstraint()
    {
        var andConstraint = Render(@<a href="#" title="GitHub Repo">Link</a>)
            .Should().HaveTitle("GitHub Repo");

        andConstraint.Should().BeOfType<AndConstraint<RenderedFragmentAssertions>>();
    }
    
    [Test]
    public void ThrowException_WhenDoesNotHaveAttribute()
    {
        Action aut = () => Render(@<a href="#">Link</a>)
            .Should().HaveTitle("GitHub Repo");

        aut.Should().Throw<AssertionException>()
            .And.Message.Should().Be("Expected IRenderedFragment to have attribute \"title\", but found <null>.");
    }
    
    [Test]
    public void ThrowException_WhenDoesNotHaveAttribute_AndShouldIncludeBecauseMessage()
    {
        Action aut = () => Render(@<a href="#">Link</a>)
            .Should().HaveTitle("GitHub Repo", "this is very important to {0}", "Steve");

        aut.Should().Throw<AssertionException>()
            .And.Message.Should().Be("Expected IRenderedFragment to have attribute \"title\" because this is very important to Steve, but found <null>.");
    }
    
    [Test]
    public void ThrowException_WhenHasAttributeWithWrongValue()
    {
        Action aut = () => Render(@<a href="#" title="GH Repo">Link</a>)
            .Should().HaveTitle("GitHub Repo");

        aut.Should().Throw<AssertionException>()
            .And.Message.Should().Be("Expected IRenderedFragment \"title\" attribute to have value \"GitHub Repo\", but found \"GH Repo\".");
    }
    
    [Test]
    public void ThrowException_WhenHasAttributeWithWrongValue_AndShouldIncludeBecauseMessage()
    {
        Action aut = () => Render(@<a href="#" title="GH Repo">Link</a>)
            .Should().HaveTitle("GitHub Repo", "this is very important to {0}", "Steve");

        aut.Should().Throw<AssertionException>()
            .And.Message.Should().Be("Expected IRenderedFragment \"title\" attribute to have value \"GitHub Repo\" because this is very important to Steve, but found \"GH Repo\".");
    }
}